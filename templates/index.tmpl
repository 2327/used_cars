{% extends "layout.tmpl" %}
{% block body %}

<section id="intro" class="intro">
        <div class="col-md-6">
                <div class="card card-body">
<br><br><br><br>
                    <h3 class="text-center mb-4">Ввод данных</h3>
                    <form action="/search" method="POST">
                    <fieldset>
                        <div class="form-group has-error">
			      <select class="form-control input-lg">
                                   <option value="brand">brand</option>

                              </select>
                        </div>
                        <div class="form-group has-success">
                            <input class="form-control input-lg" placeholder="Модель" name="model" value="" type="text">
                        </div>
                        <div class="form-group has-success">
                            <input class="form-control input-lg" placeholder="Год выпуска" name="age" value="" type="text">
                        </div>
                        <div class="form-group">
                            <input class="form-control input-lg" placeholder="Пробег" name="mileage" value="" type="text">
                        </div>
                        <input class="btn btn-lg btn-primary btn-block" value="Оценить" type="submit">
                    </fieldset>
                    </form>
                </div>
        </div>
  </section>

{% endblock %}

