{% extends "layout.tmpl" %}
{% block body %}

<section id="intro" class="intro">
        <div class="col-md-6">
                <div class="card card-body">
					<br><br><br><br>
                    <h3 class="text-center mb-4">Резульзаты поиска </h3>
						<p>Резульзаты поиска</p>
						<ul>
						{% for key, value in model.items() %}
                        <li>{{ key }}: {{ value }}</li>
                        {% endfor %}
                        </ul>
						</div>
						<div>Цена: {{ price }} </div>
					</div>
        </div>
  </section>

{% endblock %}